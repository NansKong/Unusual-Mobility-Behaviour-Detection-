# Dev A pinned dependencies (Stages 0–3 + Branch A)
# Full list including shared deps; subset needed for Dev A specifically is marked

# ── Critical version pins (do NOT change) ─────────────────────────────────────
numpy==1.26.4                  # CRITICAL: 2.x breaks filterpy Kalman filter + hdbscan
torch==2.1.2+cu118             # Matched to CUDA 11.8
torchvision==0.16.2+cu118      # Matched to torch 2.1.2
torchaudio==2.1.2              # (install separately via --index-url, see below)

# ── Dev A core dependencies ───────────────────────────────────────────────────
ultralytics==8.1.47            # YOLOv8n — Stage 1
deep-sort-realtime==1.3.2      # DeepSORT — Stage 2 (NOT the original DeepSORT repo)
filterpy==1.4.5                # DeepSORT internal Kalman filter dependency
scipy==1.13.0                  # Trajectory interpolation — Stage 3
pandas==2.2.2                  # CSV hand-off format
opencv-python==4.9.0.80        # Frame I/O
scikit-learn==1.4.2            # StandardScaler (used in step5 but good to pin now)
tqdm==4.66.4                   # Progress bars
Pillow==10.3.0                 # Image processing

# ── Shared / Dev B dependencies (pin for consistency) ────────────────────────
transformers==4.40.2           # VideoMAE — Stage 4B
accelerate==0.29.3             # Model offloading
timm==0.9.16                   # ViT backbone
hdbscan==0.8.33                # Clustering — Stage 6
matplotlib==3.8.4              # Visualisation

# ── Installation instructions ──────────────────────────────────────────────────
# Step 1 — CUDA-enabled torch (run this first, separately):
#   pip install torch==2.1.2+cu118 torchvision==0.16.2+cu118 torchaudio==2.1.2+cu118 \
#       --index-url https://download.pytorch.org/whl/cu118
#
# Step 2 — All other dependencies:
#   pip install -r requirements_dev_a.txt
#   (torch/torchvision/torchaudio lines will be no-ops after step 1)
